version: '3.8'
services:
  postgres:
    image: postgres:16
    container_name: postgres
    ports:
      - "5432:5432"
    environment:
      # Set POSTGRES_PASSWORD securely (do not commit real secrets).
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-please-set-postgres-password}"
      POSTGRES_DB: inventario